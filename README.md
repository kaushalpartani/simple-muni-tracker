# Simple Muni Tracker

A vibe-coded app that allows a user to track muni stops and their next busses.

## User set up

Make sure to set the UMO_IQ_API_KEY in your environment vars prior to running this. Some info on where you can find the key is in the .env.example file.
